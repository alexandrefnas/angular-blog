import { QuebraLinhaPipe } from './quebra-linha.pipe';

describe('QuebraLinhaPipe', () => {
  it('create an instance', () => {
    const pipe = new QuebraLinhaPipe();
    expect(pipe).toBeTruthy();
  });
});
